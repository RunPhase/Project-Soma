# 자세교정 시스템 — Python 의존성
# macOS (M칩 포함) 환경 기준
#
# 설치:
#   pip install -r requirements.txt
#
# Python 3.10+ 권장

# ── 캠 분석 파트 (내 파트) ────────────────────────────────────────────────────
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0

# ── 데스크탑 펫 UI ────────────────────────────────────────────────────────────
PyQt6>=6.4.0
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"   # macOS: 모든 Space 표시

# ── 통신 (Main Thread 파트 — 팀원, 지금은 stub) ───────────────────────────────
pyzmq>=25.0.0

# ── 아두이노 시리얼 (Thread 2 파트 — 팀원) ────────────────────────────────────
pyserial>=3.5
